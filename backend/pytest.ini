[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
filterwarnings =
    # FastAPI's own startup hook and Starlette's test client both warn on
    # import; neither is something this project can act on.
    ignore::DeprecationWarning
    ignore::UserWarning
